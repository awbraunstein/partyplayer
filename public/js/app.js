// Generated by CoffeeScript 1.3.3

requirejs.config({
  paths: {
    jquery: '/lib/js/jquery-1.9.0.min',
    underscore: '/lib/js/underscore',
    backbone: '/lib/js/backbone',
    handlebars: '/lib/js/handlebars'
  },
  shim: {
    underscore: {
      deps: [],
      exports: '_'
    },
    backbone: {
      deps: ['underscore', 'jquery'],
      exports: 'Backbone'
    },
    handlebars: {
      deps: ['underscore'],
      exports: 'Handlebars'
    }
  }
});

define(function(require, exports, module) {
  var $, Backbone, Party, PartyClientView, PartyPlayerView, SearchView, init, initClient, sampleParty, sampleSong, sampleSong2, server, utils, _;
  _ = require('underscore');
  $ = require('jquery');
  Backbone = require('backbone');
  utils = require('utils');
  server = require('server');
  Party = require('models/party');
  PartyClientView = require('views/partyClient');
  PartyPlayerView = require('views/partyPlayer');
  SearchView = require('views/search');
  initClient = function(party) {
    var $partyClient, clientView, searchView;
    $partyClient = $('#party-client');
    clientView = new PartyPlayerView({
      model: new Party(party)
    });
    $partyClient.empty().append(clientView.$el);
    clientView.render();
    searchView = new SearchView();
    return searchView.search('Trey anastasio', function(source, results) {
      console.log(source);
      return console.log(results);
    });
  };
  sampleSong = {
    source: 'Soundcloud',
    score: 4,
    uri: '/tracks/297',
    duration: 399151,
    timestamp: Date.now()
  };
  sampleSong2 = {
    source: 'Soundcloud',
    score: 3,
    uri: '/tracks/296',
    duration: 422556,
    timestamp: Date.now()
  };
  sampleParty = {
    name: 'rad party',
    loc: [39, -75],
    playing: sampleSong,
    songs: [sampleSong, sampleSong2]
  };
  init = function() {
    return $(function() {
      return initClient(sampleParty);
    });
  };
  init();
  $(function() {
    return console.log('DOM ready');
  });
  return null;
});
