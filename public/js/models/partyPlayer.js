// Generated by CoffeeScript 1.4.0

define(function(require, exports, module) {
  var $, Backbone, SOCKET_PORT, _;
  _ = require('underscore');
  $ = require('jquery');
  Backbone = require('backbone');
  SOCKET_PORT = 8080;
  return exports.partyClient = Backbone.Model.extend({
    idAttribute: '_id',
    url: function() {
      return "/party/" + this.id;
    },
    initialize: function() {
      this.socket = io.connect("http://localhost:" + SOCKET_PORT);
      return this.socket.on('addsong', function(song) {
        return this.get('songs').push(data);
      });
    },
    hasSongs: function() {
      return this.model.get('songs').length !== 0;
    },
    nextSong: function() {
      var next;
      next = _.max(this.get("songs"), function(s) {
        return s.score;
      });
      this.set("songs", _.select(this.model.get("songs"), function(song) {
        return song !== next;
      }));
      this.set("playing", next);
      return this.socket.emit('playsong', next);
    }
  });
});
